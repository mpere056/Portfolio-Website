# WI-AI-04-01: Turn Archive Cards Into Safe Doors

## Properties

| Field | Value |
| --- | --- |
| State | in-progress |
| Priority | high |
| Package | `AI-04` |
| Capabilities | `CAP-AI-006`, `CAP-AI-007` |
| Requirements | `V-10` |
| Outcome | `O-02`, `O-03` |
| Owner | Codex |
| Last update | 2026-07-18 |

## Acceptance

Structured archive cards accept only validated public source IDs, registered destination IDs, bounded depth, and allowlisted safe state; invalid or model-authored routes fail closed. One LifeInbox card rendered by the global AI opens the exact museum state and offers the canonical project-world transition without arbitrary client navigation.

## Resume Packet

- Last completed checkpoint: `LPS-06` accepted a validated public LifeInbox state record and rendered correction path.
- Current checkpoint: native structured sources and the destination registry exist, but AI responses cannot yet return a validated navigable card.
- Next exact action: define and parse the runtime archive-card payload, resolve its source and destination server-side, then render only the validated result.
- Files expected in scope: AI request/response contracts, chat route, archive-card resolver/component, provider integration, tests, evidence, and tracking.
- Safety boundary: the model never authors raw URLs or arbitrary state; the client re-resolves every destination ID.
- Stop condition: malformed-card fixtures fail closed and one LifeInbox card opens exact selected state in a browser flow.

