# WI-ART-08-01: Transform LifeInbox Through Product State

## Properties

| Field | Value |
| --- | --- |
| State | paused |
| Priority | high |
| Package | `ART-08` |
| Capabilities | `CAP-ART-008`, `CAP-ART-010`, `CAP-ART-012` |
| Requirements | `V-03`, `V-04`, `V-15`, `V-17`, `V-31`, `V-32` |
| Outcome | `O-03`, `O-04` |
| Owner | Mark and Codex |
| Branch/worktree | `main` |
| Last update | 2026-07-20 |

## Current Truth

Tracking note: the LifeInbox product-state transformation is live within Production `fce50af` and remains the product truth to preserve. Separate review is superseded by the LifeInbox slice of `ART-13`; resume only if that slice requires a correction to the established reducer or interaction contract.

The LifeInbox dynamic-scene candidate is implemented locally. Its existing synthetic reducer remains authoritative while diffuse ingress, deterministic Canvas material, local settlement, a separate organization membrane, exploded SVG trust paths, selected reminder return, and evidence depth respond independently. Reduced-motion and hidden states stop continuous work; missing Canvas leaves the vessel, CSS/SVG state, labels, controls, and evidence. Forty-four files/175 tests, content, TypeScript, lint without errors, the 35-page build, and the complete local browser flow pass.

## Intended Slice

- Preserve the synthetic local-first simulation and its explicit no-live-service boundary.
- Make empty, captured, organized, Enter, Understand, and selected system layers visually distinct through product-derived material behavior.
- Represent useful return as a causal response rather than an ambient animation.
- Preserve the approved vessel as the stable matte and semantic fallback.
- Keep LifeInbox geometry and choreography route-owned while reusing only bounded lifecycle inputs.

## Resume Packet

- Last completed artifact: [LifeInbox Dynamic Scene Packet](../../implementation-plans/2026-07-19-LifeInbox-Dynamic-Scene-Packet.md) records the implemented layers, drivers, renderer, calm/failure behavior, acceptance gates, and resume ledger.
- Known-good runtime: local working tree on 2026-07-19 passes 175 tests, content, type, lint, build, and browser state review; Production `806841d` remains the live rollback.
- Next exact action: none while paused; preserve this proof and restart only if the LifeInbox `ART-13` slice identifies a product-state regression.
- Preserve: synthetic capture reducer, local-versus-illustrative truth boundary, depth routes, repository/field-note links, project-world handoff, return URL, and static vessel fallback.
- Do not assume: the LifeInbox material language belongs to Dreamlife or Sudoku; author each later route from its own packet.

## Acceptance

- [x] Empty, captured, and organized states produce named independent material changes.
- [x] Enter and Understand increase boundary depth without losing the original specimen.
- [x] Useful return redirects material and activates a visible return response.
- [x] Existing product truth, controls, direct routes, evidence, and fallback remain intact.
- [x] Reduced-motion, hidden, and Canvas-failure states retain semantic meaning.
- [x] Model/component tests, content, type, lint, build, and full local browser interaction pass.
- [x] Commit `9c3a743` and Production LifeInbox verification pass.
- [ ] Mark reviews the route-specific transformation.

## Update History

### 2026-07-19

- Activated after Mark explicitly requested continued implementation and completion of the remaining Phase 4 work.
- Authored the route packet, pure frame/path model, eight-layer manifest, bounded Canvas field, CSS ingress/core/membrane materials, SVG boundary/return paths, and evidence response.
- Passed 44 files/175 tests, 58-node/28-relationship validation, TypeScript, lint with zero errors, the 35-page build, and the complete local browser state flow.
- Promoted the complete sequence at `9c3a743`; public LifeInbox verification is recorded by `EV-ART-11-02`.
