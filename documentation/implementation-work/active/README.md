# Active Work Items

Store `ready`, `in-progress`, `in-review`, `blocked`, and `paused` work-item files here.

The active registry is `../README.md`. Every file in this folder must appear there and contain a current resume packet.
