# WI-QA-01-02: Release The Phase 2 Exploration Shell

## Properties

| Field | Value |
| --- | --- |
| State | in-progress |
| Priority | high |
| Package | `QA-01` |
| Capabilities | `CAP-EXP-003` through `CAP-EXP-013`, `CAP-AI-003` through `CAP-AI-005`, `CAP-KG-005` through `CAP-KG-007` |
| Requirements | `V-01` through `V-12`, `V-20`, `V-22` |
| Outcome | `O-01`, `O-02` |
| Owner | Codex |
| Branch/worktree | `main` |
| Created | 2026-07-17 |
| Last update | 2026-07-17 |

## Acceptance

All Phase 2 packages pass the aggregate test, type, build, Preview, and production-safe route gates; documentation names the exact exposure boundary and Phase 3 handoff; commits are pushed and the deployed revisions are verified.

## Resume Packet

### Current Truth

- State in one sentence: All ten Phase 2 implementation packages are accepted locally; aggregate release and deployment verification are in progress.
- Works now: 123 automated cases, strict typechecking, 49-node/19-edge content validation, production build, package-level browser flows, and production feature-flag isolation pass.
- Incomplete or stubbed: Preview deployment flow, production-safe deployment, live routes, and deployment evidence.
- Safe exposure: Development and Preview enable Phase 2; Production keeps all new experience flags off.

### Known-Good Point

- Commit: `51b2e7d`.
- Branch/worktree: `main` in `C:\Projects\Portfolio-Website`.
- Verification command: `npm test && npm exec -- tsc --noEmit && npm run build`.
- Verification result: `npm test` passes 31 files and 123 tests; typecheck, content validation, and the 26-page production build pass.
- Feature flags: Phase 2 on in Development/Preview and off in Production.

### Restart Here

- Next exact action: Confirm the aggregate build, commit reconciliation, push, deploy Preview, and run the live browser matrix.
- Expected observable result: Preview demonstrates the full shell and Production keeps legacy presentation while all public routes remain healthy.
- Only after that: Close this work item and begin the Phase 3 flagship selection/prototype sequence.

### Context That Must Survive

- Production enablement is not part of this release; it requires a later combined creative review.
- `QA-04` target-device stimulation review remains later and does not make the accepted Phase 2 foundation partial.
- Preserve unrelated `.gitignore`, Husky, misc-content, `.tmp-repos/`, and `.tools/` changes.

## Implementation Checklist

- [x] Aggregate lint and 123-test suite.
- [x] Strict typecheck.
- [x] Final production build.
- [ ] Preview deployment and full browser flow.
- [ ] Production-safe deployment and live-route verification.
- [ ] Evidence, dashboard, commit, and push reconciliation.
