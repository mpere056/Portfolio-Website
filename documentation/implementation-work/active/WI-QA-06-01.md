# WI-QA-06-01: Reconcile And Strengthen The Implementation Plans

## Properties

| Field | Value |
| --- | --- |
| State | in-progress |
| Priority | high |
| Package | `QA-06` |
| Capabilities | `CAP-QA-006` |
| Requirements | Platform tracking integrity |
| Outcome | All active outcomes |
| Owner | Codex |
| Last update | 2026-07-17 |

## Acceptance

The roadmap, architecture, workstream plans, package registry, capability ledger, dashboard, evidence registry, and work-item registry agree with the deployed `PRJ-01` baseline and with each other; Phase 3 has one dependency graph with a bounded selected-flagship state gate; missing accepted evidence is registered; stale completed decisions are removed from near-term queues; and an automated integrity test fails on package-count, work-item, package-reference, or evidence drift.

## Resume Packet

- Last completed checkpoint: `PRJ-01` is implemented at `e462080`, tracking closes at `fa0d671`, production deployment `dpl_9DeyQkjizoRc91163TcF6TuumEbL` is Ready, and four public project routes return `200`.
- Current checkpoint: cross-document audit found missing Phase 2 evidence registry rows, inaccurate package counts, a stale `QA-01` capability state, and conflicting Phase 3 living-state dependencies.
- Next exact action: revise the canonical Phase 3 sequence and package ownership, then add the planning-integrity test before closing the audit.
- Files in scope: `documentation/implementation-plans/*`, `documentation/implementation-evidence/*`, `documentation/implementation-work/*`, and `tests/planningIntegrity.test.ts`.
- Do not touch: visitor-facing implementation, project content, unrelated local changes, private data, production services, or billing configuration.
- Stop condition: the integrity test and repository verification pass, evidence is registered, this work item moves to completed, and `WI-PRJ-02-01` returns to the Now slot.

## Findings To Resolve

- Ten accepted Phase 2 evidence IDs exist in package files but not the canonical evidence registry.
- Dashboard package-state counts mix package rows with feedback/later markers and miscount pending packages.
- `CAP-QA-001` remains planned despite accepted `QA-01` evidence.
- `CAP-PRJ-002` combines accepted stable anchors with unfinished URL/history/subdomain integration.
- Phase 3 requires portfolio-wide lifecycle classification in one plan but defers it in another.
- `PRJ-04` has no narrow state-seed package and its selected-project versus generalized route responsibilities are unclear.
- The museum plan still describes a pre-implementation module layout instead of the accepted `PRJ-01` boundary.
