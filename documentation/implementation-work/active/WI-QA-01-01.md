# WI-QA-01-01: Establish The Foundation Flow Harness

## Properties

| Field | Value |
| --- | --- |
| State | ready |
| Priority | high |
| Package | `QA-01` |
| Capabilities | Foundation contracts across `O-00` and `O-01` |
| Outcome | `O-00` |
| Owner | Codex |
| Created | 2026-07-16 |
| Last update | 2026-07-16 |

## Acceptance

Define one automated foundation flow that exercises route/destination resolution, context nesting, store hydration and semantic checkpoint restoration, graph validation, and safe fallbacks as an integrated contract rather than isolated units; record the browser-tool decision and preview checklist; pass the full suite and production build without mounting visitor-facing Phase 2 features.

## Resume Packet

- Current truth: Phase 1 passes 61 tests and build, but no single test proves the foundations cooperate as one flow.
- Known-good commit: `afa5f6739459a03e7f47e6cfb0273cdd55170a76`.
- Next exact action: Author an integration fixture that resolves a project destination, writes/restores its semantic checkpoint, pushes nested AI object context, and verifies only public graph neighbors are eligible.
- First files: `src/lib/destinations.ts`, `src/lib/experience/store.ts`, `src/lib/ai/context.ts`, `src/lib/content/graph.ts`, and `tests/`.
- Safe exposure: Test-only; do not mount First Note, depth UI, or the global AI shell.
- After acceptance: Prepare `EXP-02` for one controlled depth primitive.
