# WI-AI-02-01: Build The Quiet Global AI Shell

## Properties

| Field | Value |
| --- | --- |
| State | ready |
| Priority | high |
| Package | `AI-02` |
| Capabilities | `CAP-AI-003`, `CAP-AI-004` |
| Requirements | `V-09`, `V-22` |
| Outcome | `O-02` |
| Owner | shared |
| Branch/worktree | `main` |
| Created | 2026-07-16 |
| Last update | 2026-07-16 |

## Acceptance

One lightweight AI presence is mounted in the root layout behind `globalAI`, stays quiet in its dormant state, reflects context availability without repeated prompts, opens a lazy conversation surface without unmounting or obscuring the current page, preserves in-tab shell state across main-site routes, supports close/clear/error behavior, and remains usable with sound off or reduced motion. AI failure never blocks ordinary navigation.

## Resume Packet

### Current Truth

- Works now: `/chat` streams grounded replies through `ChatUI`; `AI-01` owns nested semantic context; typed flags exist; the root layout can mount global systems.
- Incomplete: No global provider/presence, lazy overlay, shell state model, route integration, or non-blocking error boundary exists. Home still links directly to `/chat`.
- Safe exposure: Build behind `globalAI`; keep Production false until cross-route browser and visual review pass; preserve `/chat` as compatibility UI.

### Known-Good Point

- Commit: `aeff727`.
- Verification: 93 tests in 24 files, strict typecheck, content validation, production build, and 42-document Firestore metadata readback pass.
- Dependencies: `AI-01`, `BAS-03`, `KG-05`, and `KG-06` are complete.

### Restart Here

- Next exact action: Define a pure dormant/context/listening/responding/error shell state model and an injectable provider contract before extracting any existing chat UI.
- First files: `src/lib/ai/context.ts`, `src/components/ChatUI.tsx`, `src/app/chat/page.tsx`, `src/app/layout.tsx`, and `src/lib/featureFlags.ts`.
- Expected result: Unit fixtures prove open/close/context/error/clear state transitions and lazy-load fallback without route or message-history coupling.
- Only after that: Mount a restrained presence and overlay in Development/Preview, then browser-test Home, About, Projects, and one site route.

## Decisions And Boundaries

- One global presence, not repeated “ask about this” buttons.
- AI context changes may alter a concise label or subtle appearance but never open the surface automatically.
- Preserve `/chat` until `AI-05`; do not force route migration in this package.
- Do not expose sources/cards yet; `AI-03` owns trusted retrieval and `AI-04` owns cards.
- The surface must be lazy and ordinary navigation must remain usable if its chunk or API fails.

## Implementation Checklist

- [ ] Define pure shell states, events, presentation, and tests.
- [ ] Add provider ownership and in-tab state boundary.
- [ ] Add restrained global presence and contextual label.
- [ ] Lazy-load an overlay/drawer without unmounting page state.
- [ ] Preserve current `/chat` compatibility route.
- [ ] Add reduced-motion, sound-off, error, retry, close, and clear behavior.
- [ ] Pass cross-route integration and browser flows behind `globalAI`.
- [ ] Reconcile evidence, capabilities, package status, and the next work item.
