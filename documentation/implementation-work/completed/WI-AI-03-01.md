# WI-AI-03-01: Wire Contextual Retrieval And Trusted Sources

## Properties

| Field | Value |
| --- | --- |
| State | done |
| Priority | high |
| Package | `AI-03` |
| Capability | `CAP-AI-005` |
| Outcome | `O-02` |
| Owner | Codex |
| Last update | 2026-07-16 |

## Acceptance

Global conversation sends semantic identifiers only; malformed/private context falls back safely; the server resolves public graph context and sources; structured source UI accepts only canonical destinations whose registered node matches the descriptor; `/chat` remains context-free compatible.

## Completion Summary

- Request trust: A bounded runtime parser validates messages and semantic IDs, ignores client copy, and rejects malformed context without rejecting an otherwise valid conversation.
- Retrieval: Accepted `nodeId` enters the `KG-06` bounded rerank; unknown/private nodes produce ordinary vector order.
- Sources: Native AI SDK `StreamData` carries at most four public descriptors, with no model-text markers or duplicate retrieval request.
- Client validation: Destination ID, href, target origin, canonical status, and registered node ID must all match before a source becomes a link.
- Compatibility: Context-free calls preserve the original `fetchContext(query, topK)` signature and `/chat` behavior; Gemini generation fallback remains intact.
- Verification: Request/source/chat fixtures pass, including malformed context, structured data, generation fallback, canonical destination enforcement, and source limits. Final Phase 2 gate will rerun the complete suite, typecheck, build, Preview, and browser flow.
- Evidence: `EV-AI-03-01`.
- Implementation: `aeb3152`.
- Safe exposure: Source UI ships only where `globalAI` is enabled; Production flag remains off.
- Remaining work moved to: `WI-EXP-04-01` for the non-linear guided tour.
