# ART-07 Evidence

## EV-ART-07-01: Dynamic Museum Scene Candidate

Status: candidate.

The Museum now has a pure deterministic scene model and a seven-layer executable manifest. The approved ecology WebP remains the stable checksum and fallback. Masked raster/CSS membrane and aperture layers respond independently; a bounded Canvas 2D field gathers toward the active signal; SVG filaments and DOM readouts render only public reviewed graph relationships; selected projects carry their own material plate into Approach. Reduced-motion and hidden-state logic stop continuous work without removing semantic content.

Verification on 2026-07-19:

- 43 test files and 171 tests pass, including four new scene-model tests and updated Museum graph/render fixtures.
- TypeScript passes.
- ESLint reports zero errors; ten pre-existing warnings remain outside this package.
- Content validation passes with 58 nodes and 28 relationships.
- The 35-page Next.js production build passes after the existing Google Fonts are reachable.
- Local in-app browser review passes for the lobby, LifeInbox selection, project-specific Approach material, reviewed `relationship:lifeinbox-documented-in-trust-post` content, return control, and zero browser console warnings/errors.

This evidence does not claim Preview, Production, explicit GPU/context-loss testing, or Mark creative acceptance. Production `806841d` remains the known-good live fallback until the candidate is committed and deployed.
