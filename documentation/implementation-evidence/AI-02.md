# AI-02 Evidence

Last updated: 2026-07-16

## Package Snapshot

| Field | Value |
| --- | --- |
| Package | `AI-02` Quiet global shell |
| Lifecycle | complete |
| Capabilities | `CAP-AI-003`, `CAP-AI-004` |
| Work item | `WI-AI-02-01` |
| Implementation | `3dfef6b` |
| Exposure | Development and Preview on; Production off |

## EV-AI-02-01: Shell State And Ownership Contract

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | integration-test |
| Claim | One provider owns route-aware context and deterministic shell state while preserving the Phase 1 selector API; close cannot be undone by late chat activity. |
| Evidence | `tests/aiShell.test.ts`, `tests/portfolioAIProvider.test.tsx`, and feature-flag fixtures pass; strict typecheck and lint pass. |

The dormant presence uses no 3D runtime, conversation loads only when opened, dynamic/render errors are isolated, and `/chat` is excluded from duplicate global UI.

## EV-AI-02-02: Cross-Route Browser And Visual Flow

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | browser-flow and visual-review |
| Environment | Local Development with `globalAI=true` |
| Claim | Home has one spatial AI doorway; the panel opens without route loss, survives Home-to-About navigation, clears and closes, About regains one quiet presence, audio remains optional/non-blocking, and `/chat` stays unchanged. |
| Evidence | DOM-backed in-app browser flow plus panel screenshot review; production build `hePSAMB5PTGVw9uoxDB_S`. |

The browser flow initially exposed close/input interception by the legacy full-screen audio gate. `globalAI` now selects a non-blocking audio prompt, satisfying the sound-off boundary without changing Production while the flag is false.

## Boundaries After Completion

- Sources and current-node request wiring remain `AI-03`.
- Cards and destination transitions remain `AI-04`.
- `/chat` migration remains `AI-05`.
- Production promotion still requires Mark's creative review.

Package complete. `AI-03` is ready.
