# QA-01 Evidence

Last updated: 2026-07-17

## Package Snapshot

| Field | Value |
| --- | --- |
| Package | `QA-01` Foundation test harness |
| Lifecycle | complete |
| Work item | `WI-QA-01-01` |
| Implementation | `4c9b02a` |
| Exposure | Test-only; no visitor-facing component mounted |

## EV-QA-01-01: Integrated Foundation Flow

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | integration-test |
| Claim | A canonical project destination, reviewed public graph relationships, semantic persistence/restore, nested AI context, and safe fallbacks cooperate as one flow. |
| Evidence | `tests/foundationFlow.test.ts`; full suite passes 68 tests in 19 files; strict typecheck, content validation, and production build pass. |

The flow resolves Dreamlife in the museum, selects only reviewed public relationships, persists and restores a `handle` checkpoint, composes and unwinds nested AI relationship context, falls back from an unavailable destination, and rejects an external context route.

## EV-QA-01-02: Aggregate Phase 2 Local Gate

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | aggregate test, type, content, and build gate |
| Claim | Every Phase 2 package compiles and cooperates within the complete repository without weakening graph validation, legacy routes, or production feature isolation. |
| Evidence | `npm test` passes 31 files and 123 tests after repository lint; `npm exec -- tsc --noEmit` passes; `npm run build` validates 49 nodes and 19 relationships, compiles Next.js 16.2.10, and generates all 26 static pages. |

## EV-QA-01-03: Protected Preview And Live Retrieval

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | Preview deployment, protected HTTP matrix, and live integration |
| Deployment | `dpl_4nL6kUcQjAfUjKdkNdCrDw4giwv9`; `https://portfolio-website-4a57q6vgq-marks-projects-445b0eb9.vercel.app` |
| Claim | The exact clean commit builds on Vercel; all representative routes return `200`; live Firebase retrieval and Gemini generation return native validated Dreamlife sources. |
| Evidence | Vercel Ready state; authenticated protected checks for Home, About, Projects, Chat, Dreamlife, LifeInbox blog, and Sudoku blog; `/api/chat` returns `200`, grounded text, `project:dreamlife`, and canonical `/projects#dreamlife` destination data. |

Vercel Authentication intentionally protects anonymous Preview browsing. Package-level interactive and visual flows therefore remain the accepted browser evidence; the live Preview gate adds deployment, route, environment, retrieval, and transport proof without changing project access settings.

## EV-QA-01-04: Production-Safe Promotion And Public Routes

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | production deployment, public browser, route matrix, and live integration |
| Deployment | `dpl_51xD2i8nQU8zEgjFiHnvNCvB2JqA`; aliased to `https://www.marknperera.ca` |
| Claim | The same clean commit is Ready in Production; the apex redirects safely; main and project routes remain public; AI credentials work; new Phase 2 visitor UI remains off. |
| Evidence | Apex, About, Projects, LifeInbox, Sudoku Together, Dreamlife, and all three project blog routes return `200`; browser inspection shows distinct LifeInbox/Sudoku headings and blog links; production `/api/chat` returns `200` with canonical Dreamlife structured sources. |

## Browser And Preview Decision

- Keep this foundation gate deterministic and browser-independent because no UI is mounted.
- Use automated browser tests when `EXP-02` mounts a controlled component and for First Note, tour, global AI, and history behavior.
- Prefer CLI deployment inspection and HTTP route checks for dormant infrastructure; do not use mouse-controlling browser plugins for non-visual checks.
- Deployment checklist: exact commit is Ready, main/project aliases are attached, main and project home/blog routes return `200`, and current visuals remain unchanged.

Package complete. The active visitor-foundation work moves to `EXP-02`.
