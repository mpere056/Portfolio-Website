# QA-01 Evidence

Last updated: 2026-07-16

## Package Snapshot

| Field | Value |
| --- | --- |
| Package | `QA-01` Foundation test harness |
| Lifecycle | complete |
| Work item | `WI-QA-01-01` |
| Implementation | `4c9b02a` |
| Exposure | Test-only; no visitor-facing component mounted |

## EV-QA-01-01: Integrated Foundation Flow

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | integration-test |
| Claim | A canonical project destination, reviewed public graph relationships, semantic persistence/restore, nested AI context, and safe fallbacks cooperate as one flow. |
| Evidence | `tests/foundationFlow.test.ts`; full suite passes 68 tests in 19 files; strict typecheck, content validation, and production build pass. |

The flow resolves Dreamlife in the museum, selects only reviewed public relationships, persists and restores a `handle` checkpoint, composes and unwinds nested AI relationship context, falls back from an unavailable destination, and rejects an external context route.

## Browser And Preview Decision

- Keep this foundation gate deterministic and browser-independent because no UI is mounted.
- Use automated browser tests when `EXP-02` mounts a controlled component and for First Note, tour, global AI, and history behavior.
- Prefer CLI deployment inspection and HTTP route checks for dormant infrastructure; do not use mouse-controlling browser plugins for non-visual checks.
- Deployment checklist: exact commit is Ready, main/project aliases are attached, main and project home/blog routes return `200`, and current visuals remain unchanged.

Package complete. The active visitor-foundation work moves to `EXP-02`.
