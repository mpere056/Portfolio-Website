# AI-04 Evidence

Last updated: 2026-07-18

## Package Snapshot

| Field | Value |
| --- | --- |
| Package | `AI-04` Archive cards and selected-destination flow |
| Lifecycle | complete |
| Capabilities | `CAP-AI-006`, `CAP-AI-007` |
| Work item | `WI-AI-04-01` |
| Implementation | `17b4491` |

## EV-AI-04-01: Fail-Closed Archive-Card Contract

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | contract and privacy-security |
| Claim | A card can contain only bounded reviewed copy, public source node IDs, one canonical registered destination, supported depth, and allowlisted primitive state. The client ignores any raw URL and re-resolves the destination. |
| Evidence | `tests/archiveCards.test.tsx` covers the valid LifeInbox door plus unknown destination, mismatched source, unsupported state, depth mismatch, overlong copy, and injected hostile URL. |

## EV-AI-04-02: Live Selected-Destination Response

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | preview and integration-test |
| Environment | Vercel Preview `dpl_BGDDAtggEihci1DMz4zj1r1vAS9X` |
| Claim | A real contextual LifeInbox request resolves public Firebase sources, streams a grounded Gemini answer, and returns the exact Handle card; the card resolves to `/projects?stage=handle#lifeinbox` and records the semantic checkpoint before navigation. |
| Evidence | Protected `POST /api/chat` returned `project:lifeinbox`, `archive-card:lifeinbox-capture-boundary`, registered museum destination, `requestedDepth=handle`, and `{stage: handle}`; component and persistence behavior pass local tests. |

## Boundaries After Completion

- The model still cannot author URLs or arbitrary client state.
- One reviewed card is intentionally implemented; a card gallery and `/chat` migration remain `AI-05` or later.
- Local live generation was unavailable inside the network sandbox and correctly showed the archive-paused fallback; the protected Preview supplied the external-service proof.

