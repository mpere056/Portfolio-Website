# KG-06 Evidence

Last updated: 2026-07-16

## Package Snapshot

| Field | Value |
| --- | --- |
| Package | `KG-06` Graph-aware retrieval metadata |
| Lifecycle | complete |
| Capability | `CAP-KG-007` |
| Work item | `WI-KG-06-01` |
| Implementation | `7597c1b`, `aeff727` |
| Exposure | Additive server retrieval contract; context not yet sent by current chat UI |

## EV-KG-06-01: Metadata, Context, And Public Source Contract

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | integration-test |
| Claim | Public graph metadata is generated deterministically; private/draft nodes are excluded; old documents remain readable; validated context is bounded; selected IDs resolve to public descriptors. |
| Evidence | `tests/ragMetadata.test.ts`, `tests/ragStore.test.ts`, and `tests/retriever.test.ts` pass within the 93-test suite; strict typecheck, content validation, and production build pass. |

The direct vector order remains primary. Exact context receives a two-position maximum nudge, reviewed neighbors receive one, and unknown node IDs leave order untouched.

## EV-KG-06-02: Free Firestore Metadata Backfill

| Field | Value |
| --- | --- |
| Status | accepted |
| Type | integration-test and production data operation |
| Environment | `mark-portfolio-ai-mpere056` Spark Firestore under `mpere056@gmail.com`; billing unchanged |
| Claim | Every canonical retrieval chunk stores and returns the additive metadata without changing embedding vectors or requiring Gemini re-embedding. |
| Evidence | `npm run backfill:rag-metadata` via active gcloud token: 42 updated, 42 verified, zero stale/artifact/unmatched, 42 identified documents remain. |

The initial URL-PATCH approach created 42 metadata-only raw-colon IDs. They were detected by an exact no-content/metadata-only/canonical-twin predicate, deleted, and followed by a clean readback. This failed attempt is retained here because it materially changed the verification and cleanup tooling.

## Boundaries After Completion

- Current `/api/chat` still calls retrieval without a context options object; `AI-03` owns request validation and source delivery.
- Firestore remains Spark/free with deny-all client rules and server-only access.
- Content freshness versions remain `LPS-04`; no synthetic version was invented.

Package complete. `AI-02` is ready independently of API context wiring.
